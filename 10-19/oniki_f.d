module oniki_f;import std.stdio;

void main(){
    int sayı;
    bool doğru=true;
    while(doğru){
        ++sayı;
        if(sayı==10)
        {
        doğru=false;
        }
        if(sayı!=7)
        {
            write(sayı, " ");
        }
    }
}

// KENDİMLE GURUR DUYUYORUM.