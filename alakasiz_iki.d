module alakasiz_iki;import std.stdio;

void main(){
    writeln("maks değeri:", double.max);
    writeln("min değeri:", -double.max);
}

//".min" niteliği kesirli sayı türleri ile kullanılmaz.
//Kesirli sayı türlerinin minimum değerini bulmak için yukarıdaki örnekte
//olduğu gibi "max" niteliğinin zıt işaretlisi kullanılır.